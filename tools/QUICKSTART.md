# 🚀 快速使用指南

## 一分钟上手

### 第一步: 安装
双击运行 `install.bat`,等待安装完成

### 第二步: 转换
```bash
convert.bat <你的Artifact链接>
```

### 第三步: 导入
将生成的`artifacts`文件夹导入到Anytype

## 完整示例

假设你要转换这个文档:
```
https://g.co/gemini/share/83a6cfa7-6e28-49e9-a43f-69b350435889
```

### 操作步骤:

1. **打开命令行**
   - 按 `Win + R`
   - 输入 `cmd`
   - 回车

2. **进入tools目录**
   ```bash
   cd D:\编程\项目\AiVideoAnalsysSystem\tools
   ```

3. **运行转换命令**
   ```bash
   convert.bat https://g.co/gemini/share/83a6cfa7-6e28-49e9-a43f-69b350435889
   ```

4. **等待完成**
   - 浏览器会自动打开
   - 脚本会自动提取内容
   - Mermaid图表会自动转换为PNG
   - 完成后会显示保存路径

5. **导入到Anytype**
   - 打开生成的`artifacts`文件夹
   - 将整个文件夹拖入Anytype
   - 完成! 🎉

## 常见使用场景

### 场景1: 转换单个文档
```bash
convert.bat https://g.co/gemini/share/xxxxx
```

### 场景2: 指定文件名
```bash
python artifact_to_markdown.py https://g.co/gemini/share/xxxxx -f "架构设计文档.md"
```

### 场景3: 批量转换
创建 `my_batch.bat`:
```batch
@echo off
call convert.bat https://g.co/gemini/share/doc1
call convert.bat https://g.co/gemini/share/doc2
call convert.bat https://g.co/gemini/share/doc3
pause
```

## 输出文件结构

```
artifacts/
├── artifact_20231224_205800.md    ← 主文档
└── images/                         ← 图片文件夹
    ├── mermaid_1_20231224_205800.png
    ├── mermaid_2_20231224_205800.png
    └── mermaid_3_20231224_205800.png
```

## 注意事项

⚠️ **重要**: 
- 确保网络连接正常(需要访问mermaid.ink)
- 不要移动或删除images文件夹
- Markdown文件和images文件夹必须在同一目录

✅ **提示**:
- 可以重命名artifacts文件夹
- 可以重命名Markdown文件
- 但不要改变images文件夹的相对位置

## 遇到问题?

### 问题1: 安装失败
**解决**: 
1. 确保已安装Python 3.7+
2. 手动运行: `pip install playwright`
3. 然后运行: `playwright install chromium`

### 问题2: 图表渲染失败
**解决**:
1. 检查网络连接
2. 尝试使用VPN
3. 脚本会自动保留原始Mermaid代码

### 问题3: 内容提取不完整
**解决**:
1. 确保Artifact链接正确
2. 等待页面完全加载
3. 查看控制台输出的错误信息

## 需要帮助?

查看完整文档: `README.md`

---

**祝你使用愉快! 🎉**
